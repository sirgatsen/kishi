<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layout_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/menu"
    tools:context="com.gatsensoft.kishi.MenuActivity">

    <Button
        android:id="@+id/btnReadBook"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:onClick="readBook"
        android:text="Read Book"
        android:layout_centerVertical="true"
        android:layout_alignStart="@+id/btnPlayGame"
        android:layout_alignLeft="@+id/btnPlayGame" />

    <Button
        android:id="@+id/btnPlayGame"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/btnReadBook"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        android:onClick="playGame"
        android:text="Play Game" />

</RelativeLayout>